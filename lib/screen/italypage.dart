import 'package:flutter/material.dart';

class ITALYPage extends StatelessWidget {
  const ITALYPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Italy'),
      ),
    );
  }
}
