import 'package:flutter/material.dart';

class FRANCEPage extends StatelessWidget {
  const FRANCEPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FRANCE'),
      ),
    );
  }
}
