import 'package:flutter/material.dart';

class UKPage extends StatelessWidget {
  const UKPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('UK'),
      ),
    );
  }
}
